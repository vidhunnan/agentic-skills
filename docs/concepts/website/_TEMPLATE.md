# Exploration — {thread title}

Status: {active | closed} · Started: {YYYY-MM-DD}

> Append-only. Rounds below are frozen; new rounds go at the bottom. **A killed
> direction is never deleted** — that record is the point of this file.

## Round {N} — {title}

**Date:** {YYYY-MM-DD}
**Testing:** {the question this round was trying to answer — not a description of
what you made. "Whether the rail holds six items without a scroll" is a test;
"a new sidebar" is not.}
**Links:** {frame / version / prototype URLs, verbatim} — or `*(none)*`

### What changed

{What was different from the previous round. On round 1, the starting point.}

### What we learned

{The finding — or `*(nothing conclusive)*`. Most rounds genuinely teach nothing
crisp, and a log that pretends otherwise is a log full of invented insight.}

### Verdict

**{Kept | Killed | Parked}** — {why}

{**Killed** is the valuable one; push for the reason. **Parked** exists so that
"we stopped working on it" has somewhere honest to go instead of everything
defaulting to Kept. If the reason was never articulated, write
`*(reason not stated)*` — do not reconstruct one.}

---

_Append-only. Never edit or delete a prior round. If a killed direction comes back,
that's a new round that references the old one — not a rewrite of history._
