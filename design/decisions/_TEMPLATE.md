# {NNNN}. {The decision, as a statement}

- **Status:** Proposed | **Accepted** | Superseded by [{NNNN}](./{NNNN}-{slug}.md)
- **Date:** {YYYY-MM-DD — when the decision was **made**, from the evidence. Not the
  day you logged it. Append *(approx.)* if inferred.}
- **Supersedes:** [{NNNN}](./{NNNN}-{slug}.md) *(omit if none)*

## Context

{What was true when this came up. The situation, the constraint, the pressure —
never a motive nobody wrote down.}

## Options considered

| Option | The bet | Why it lost |
|---|---|---|
| {A} | {…} | {…} |
| **{B} — chosen** | {…} | — |

{**If you cannot name a single alternative a reasonable person would have picked,
this is not a decision** — it's a preference or a task, and it does not belong in
this folder.}

## Decision

{What was chosen. One paragraph, active voice.}

## What we gave up

{The cost. Required. Every design choice is a trade — this is the traded-away half,
and it is the first thing anyone asks about a year later. `*(none identified)*`
only after you've pushed once; a choice with no cost usually means the loser was
never real.}

## What would make us revisit

{The condition. "If mobile passes 60% of sessions" is a condition; "if it stops
working" is not. `*(not stated)*` is fine.}

## Evidence

- **Primary:** {this conversation, {date} | `../explorations/{thread}.md` round N |
  `../research/{study}.md` | `../briefs/{slug}.md`}
  > {short verbatim quote — paths rot, this file is forever}
- **Rationale:** stated at the time | supplied by {who} on {YYYY-MM-DD} (not written
  down at the time) | **`*(reason not stated)*` — no reason was ever recorded**

## Follow-up

*Append-only. Everything above is **frozen**. Entries below are dated and additive.*

*(none yet)*

---

_**Exactly two things in this file may ever change:** the `**Status:**` line and
additions under `## Follow-up`. If this turns out to be wrong, do not edit it —
supersede it with a new decision that links back._
