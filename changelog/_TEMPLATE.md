# {commit subject}

- **Commit:** `{full-hash}` (`{short-hash}`)
- **Author:** {author}
- **Date:** {YYYY-MM-DD}
- **PR:** #{NN}

## Commit message

{verbatim commit message body}

## Changes in detail

### `{path/to/file}` (new | modified | deleted)

- What changed in this file.
- Why it changed — from the commit message, or the evident intent of the diff. If
  the rationale genuinely isn't recoverable, say so. **Never invent one.**

### `{path/to/another}`

- …

## Files changed

```
{git diffstat output, verbatim}
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.** Never invent a rationale, a
file, or a hash. Omit the `**PR:**` line entirely if there's no PR number. Do not
hand-edit files in this folder._
