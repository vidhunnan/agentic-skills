# docs(design): log exploration round 2 — the register held at full content

- **Commit:** `ac8dc7076a65ee334eb9d16cd2dac146e29a42f8` (`ac8dc70`)
- **Author:** Vidhunnan Murugan
- **Date:** 2026-08-19

## Commit message

Round 1 could only test register: its comps carried a fifth of the page, so density,
rhythm and a fourteen-row catalogue were never exercised. Round 2 rebuilt the winner
at full content to answer the one question round 1 couldn't.

Kept, approved for build. Round 1's stated reason carries; no new reason was
articulated and none is invented here.

What the round learned, beyond the verdict: recessive syntax turned out to be the
strongest available reading of a constraint that already existed. The language doc
requires records "shown verbatim, never restyled" — showing ## and *(reason not
stated)* dimmed rather than hidden IS verbatim, where rendering them into styled
components would not be. The direction and the constraint are the same idea, which
was not obvious when either was written.

One craft variable left unresolved and recorded as the main risk: how dim recessive
should be. It was set by eye in a single pass, and it is the line between "this is
the file" and "this is a costume".

## Changes in detail

### `docs/concepts/website/website-revamp.md` (modified)

- One round appended, above the closing "What this round cost" section. Round 1 is
  untouched, which is what the file's append-only rule requires.
- §Testing states the question round 1 could not answer — does terminal-rendered
  markdown survive a full page? — and why density, rhythm and a fourteen-row catalogue
  are exactly what a thin comp cannot show. Links the artifact and `git:5e1d1ca`.
- §What changed records the scope round 1 lacked: all 14 skills, all 9 matrix rows,
  six sections, nav and footer, 19KB against ~6.5KB, and the two sections authored for
  the first time — the worked loop and install as a sequence.
- §What we learned records three findings. The register **held**: the failure mode a
  monospace page is most exposed to — a long catalogue turning into an undifferentiated
  wall — did not appear at fourteen rows, because the compact index carries most of
  them at one line each. Recessive syntax turned out to be the strongest reading of a
  constraint that already existed, so the direction and the constraint are the same
  idea, which was not obvious when either was written. And one craft variable is
  unresolved and named as the main risk: how dim *recessive* should be, set by eye in a
  single pass, the line between *this is the file* and *this is a costume*.
- §Verdict: **Kept**, approved for build. States that no reason beyond round 1's was
  separately articulated and **none is invented here** — round 1's stated reason carries.

## Files changed

```
 docs/concepts/website/website-revamp.md | 54 +++++++++++++++++++++++++++++++++
 1 file changed, 54 insertions(+)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
