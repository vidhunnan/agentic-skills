# Add the expansion roadmap and the design context stack concepts

- **Commit:** `3cc7b687f20174c34cd6e46ed93495192028613d` (`3cc7b68`)
- **Author:** Claude
- **Date:** 2026-08-16

## Commit message

Fills docs/concepts/, which had a README and a template and no entries.

- skill-library-expansion.md: the four territories, the proposed grouping,
  and the 6 -> 32 roadmap.
- design-context-stack.md: the case that design is the strongest case for
  this library rather than a side quest, the seven design tiers, and the
  twenty skills that would fill them.

Roadmapped skills are filed as concepts rather than PRDs on purpose: a PRD
is "a concept that graduated", and twenty-two specs for unbuilt skills
would be the exact done-vs-explored violation this repo argues against.

## Changes in detail

### `docs/concepts/skill-library-expansion.md` (new)

- The library's first roadmap: four territories (finish the context stack, design practice, meta skills that build skills, knowledge work past the repo), a seven-group structure, and a 6 → 32 skill count.
- Records three known problems the expansion should close, so they survive between sessions: merge-landed commits escaping the changelog (commit `f52b824`), the `repo-setup` / `handoff-generator` handoff-path divergence, and `CONTRIBUTING.md` documenting six touchpoints instead of seven.
- Carries the reasoning for filing unbuilt skills as concepts rather than PRDs, later logged as ADR 0019.

### `docs/concepts/design-context-stack.md` (new)

- Argues that design is where this library's thesis bites hardest: code has `git log`, and a Figma file is a snapshot of the winner that records neither the rejected directions nor the reasoning.
- Defines seven design tiers and the two distinctions they exist to preserve — evidence vs. interpretation, and what was tried vs. what won.
- Lists twenty skills across seven phases of design work, with a build order and an explicit scope limit: none of them makes a design decision or generates a visual.
- States a kill condition for the whole territory: if `design/explorations/` is still empty after a full project cycle, the problem was never tooling.

## Files changed

```
 docs/concepts/design-context-stack.md    | 210 +++++++++++++++++++++++++++++++
 docs/concepts/skill-library-expansion.md | 135 ++++++++++++++++++++
 2 files changed, 345 insertions(+)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
