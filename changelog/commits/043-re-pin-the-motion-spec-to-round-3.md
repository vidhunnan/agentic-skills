# docs(design): re-pin the motion spec to round 3

- **Commit:** `d7c630e1c5939231dba3e4070288b160fcb513ca` (`d7c630e`)
- **Author:** Vidhunnan Murugan
- **Date:** 2026-08-19

## Commit message

The step highlight and the nav's active-section marking are built, so they move out
of "Not yet built" and into the tables. Neither is an exception to the layout rule —
both follow scroll position and do nothing on their own — so the count of exceptions
stays at one.

The rail this section previously promised was cut before shipping. Recorded as cut
rather than deleted: a spec that quietly drops what it promised is how a reader stops
trusting it.

## Changes in detail

**One file, twenty insertions.** The motion spec was last re-pinned by `e05015d`
(entry 039), which recorded round 3's two behaviours under a `## Not yet built`
heading with the instruction *"fold it in when round 3 ships and re-pin the version."*
Round 3 shipped in `467dc5b` (entry 042). This does exactly that.

### `design/specs/motion.md` (modified, +20/−10)

- **Re-pinned `git:fcea6dd` → `git:467dc5b`.** The `**Source:**` line gains
  `website/components/LoopSteps.tsx` and `website/components/NavLinks.tsx` alongside
  `globals.css` and `Reveal.tsx` — the two components round 3 added.
- **The token table gains two rows.** *Step highlight* at `0.35s ease`, on background
  and left border. *Nav mark* at **`*(none)*`** — the active section is a colour swap,
  untransitioned. The second is the more interesting entry: it puts a state change on
  the record precisely by declaring that it has no duration, rather than leaving it
  undocumented because it is not an animation.
- **The states table gains two rows**, matching: the loop step passing the middle of
  the viewport (`background`, `border-left-color`, 0.35s, no stagger, meaning *"this is
  the step you are reading"*), and a section entering the viewport changing the nav link
  colour (no transition, meaning *"where you are in the document"*).
- **`## Not yet built` becomes `## Round 3, now built`.** The spec no longer carries a
  "Not yet built" section at all — the one item it held has shipped.
- The exception argument is restated rather than assumed: both behaviours *"track
  scroll position and do nothing on their own, which is the definition of
  reader-caused,"* so **the count of exceptions to the Layout rule stays at one**. The
  Layout rule itself is not modified in the diff, which is consistent with that claim.
- **The cut rail is recorded as cut, not deleted.** The previous draft of this section
  promised a sticky rail carrying the active-section marker; the new text states it was
  cut before it shipped, that it *"read as documentation furniture,"* and that the nav
  absorbed the job. The reason for keeping the note at all is stated in the file: *"a
  spec that quietly drops what it promised is how a reader stops trusting it."*
- **Not mentioned in the commit message:** a new **"Nothing types itself"** paragraph —
  no replay, no typewriter, no simulated terminal session. It cites
  `design/system/language-website.md` as the hard constraint, notes it fails all three
  of design ADR 0006's tests, and records that it *"is what killed the console demos in
  two earlier drafts."* It closes by placing the new behaviour against that line: *"The
  step highlight is the closest thing on the page to a sequence, and it only ever
  follows the reader."* The message describes only the table moves and the rail, so this
  addition would otherwise be invisible to anyone reading the log rather than the diff.
- Two rows in this commit correspond exactly to the two `LoopSteps.tsx` and
  `NavLinks.tsx` in-file arguments recorded in entry 042 — the spec and the code now
  make the same claim in the same words.

## Files changed

```
 design/specs/motion.md | 30 ++++++++++++++++++++----------
 1 file changed, 20 insertions(+), 10 deletions(-)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
