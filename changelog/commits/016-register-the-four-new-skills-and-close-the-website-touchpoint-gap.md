# Register the four new skills and close the website touchpoint gap

- **Commit:** `60c2fd300ab49c7248575af032450f84582671e2` (`60c2fd3`)
- **Author:** Claude
- **Date:** 2026-08-16

## Commit message

Adds the four skills to the marketplace, the README (two new groups --
"Design work" and "Build the skills themselves"), the install block, and
website/components/lib/skills.ts.

Also fixes a real gap: CLAUDE.md and CONTRIBUTING.md both documented adding
a skill as six steps, omitting the website's SKILL_GROUPS entry. Following
the documented process left the published site silently stale. Both now
list seven steps plus the verification pass, and both point at
/skill-scaffold first.

## Changes in detail

### `.claude-plugin/marketplace.json` (modified)

- Four new plugin entries — `skill-scaffold`, `design-setup`, `design-brief`, `design-decisions` — each with `source` pointing at its skill folder. Verified with `python3 -m json.tool`, along with all ten `plugin.json` files.

### `README.md` (modified)

- Two new groups in the Skills section: **Design work** (three skills, with the note that code has `git log` and design has nothing) and **Build the skills themselves** (`skill-scaffold`).
- Skill count in the intro goes from six to ten; the Install block gains four lines.
- Adds a pointer to `docs/concepts/` for where the library is going next.
- Corrects the ADR count in the "this repo runs on its own skills" section from 14 to 21 — it had been stale since ADRs 0015–0018 landed.

### `CLAUDE.md` (modified)

- "Adding a new skill" goes from five steps to seven, adding the `website/components/lib/skills.ts` entry and a verification step, and leads with `/skill-scaffold`.
- The README group list in step 5 now names all five groups.

### `CONTRIBUTING.md` (modified)

- Same seventh touchpoint added to the skill conventions list, called out as the step that used to be undocumented.
- Validation section gains the site build, and now asks contributors to trigger a new skill **two ways** — the slash form and a natural phrase from its description — because a description that doesn't match is a silent failure in the wild.

### `website/components/lib/skills.ts` (modified)

- Two new `SkillGroup` entries with four `Skill` objects. The file is typed, so the site build is the check: `npm run build` passes with TypeScript clean.

## Files changed

```
 .claude-plugin/marketplace.json  | 20 ++++++++++++++++++++
 CLAUDE.md                        |  6 +++++-
 CONTRIBUTING.md                  |  8 ++++++--
 README.md                        | 28 ++++++++++++++++++++++++----
 website/components/lib/skills.ts | 40 ++++++++++++++++++++++++++++++++++++++++
 5 files changed, 95 insertions(+), 7 deletions(-)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
