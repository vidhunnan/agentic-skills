# docs(changelog): catch up 032–037 and register the handoff protocol

- **Commit:** `fcc75435f81c68f293c8e4e4e62faa22f6c2d4a4` (`fcc7543`)
- **Author:** Vidhunnan Murugan
- **Date:** 2026-08-19

## Commit message

The changelog stopped at 031. Six commits shipped after it — the brief and
language doc, both exploration rounds, both round logs, and the rebuild —
and none were documented. Every entry is pulled from git, not from memory.

037 is the large one: it documents the rebuild that deleted five components
and 1,475 lines, and it records something the rebuild left behind. Three of
the code comments it added cite design ADRs 0008, 0010 and 0011, which did
not exist on disk at the time. The entry states the citation as a fact about
the code rather than asserting the records existed.

Also registers the handoff-generator protocol block in CLAUDE.md. It was the
one skill enforcing a convention here without a block declaring it.

## Changes in detail

**A note on why this commit is documented at all.** The changelog protocol omits
changelog-only commits, and six of this commit's eight files are changelog files.
It is documented because the eighth is `CLAUDE.md`: registering a skill protocol
block is a convention change in this repo, and that is the substantive half. The
six catch-up entries are **not** re-described below — they document commits
`acdd72b` through `fcea6dd`, which are entries 032–037 and are their own record.

### `CLAUDE.md` (modified)

- Registers the `skill:handoff-generator` protocol block — five lines, the only
  non-changelog change in the commit. The commit message states the gap it closes:
  it was *"the one skill enforcing a convention here without a block declaring it."*
- The block sits between `skill:model-strategy` and `skill:repo-setup`, in the
  marker format the shared registration mechanism specifies, and states three
  things: generate a brief with `/handoff-generator` when work moves between
  Claude.ai chat and Claude Code or to another session, briefs live at
  `handoff/handoff-{from}-to-{to}-{date}-{slug}.md`, and check `handoff/` for the
  latest relevant brief when resuming.

### `changelog/CHANGELOG.md` (modified)

- Six rows added to the top of the per-commit table, 032–037, newest-first. The
  dated `## 2026-08-19` section is extended rather than replaced: five entries into
  `### Added`, four into `### Changed`, five into `### Fixed`, and a `### Removed`
  section created with two — it had not existed for that date. Twenty-four insertions,
  sixteen entries.

### `changelog/commits/032`–`037` (new)

- Six per-commit entries, 734 insertions across the eight files. The largest is
  **037** at 220 lines, documenting the rebuild that deleted five components and
  1,475 lines.
- The commit message records what 037 caught in the code it was documenting: three
  of the comments the rebuild added cite *design ADR 0008*, *0010* and *0011*, and
  **those files did not exist on disk at the time**. The entry states the citation
  as a fact about the code rather than asserting the records existed — the gap those
  citations name is what commit `e05015d` (entry 039) closes.

## Files changed

```
 CLAUDE.md                                                                                   |   5 +++
 changelog/CHANGELOG.md                                                                      |  24 +++++++++++
 changelog/commits/032-close-the-positioning-brief-open-the-revamp-brief-and-language-doc.md | 120 +++++++++++++++++++++++++++++++++++++++++++++++++++++++
 changelog/commits/033-three-directions-for-the-revamp.md                                    | 104 +++++++++++++++++++++++++++++++++++++++++++
 changelog/commits/034-log-exploration-round-1-and-design-adr-0007.md                        | 133 ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 changelog/commits/035-round-2-terminal-rendered-markdown-at-full-content.md                 |  68 +++++++++++++++++++++++++++++++
 changelog/commits/036-log-exploration-round-2.md                                            |  60 ++++++++++++++++++++++++++++
 changelog/commits/037-rebuild-in-terminal-rendered-markdown.md                              | 220 ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 8 files changed, 734 insertions(+)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
