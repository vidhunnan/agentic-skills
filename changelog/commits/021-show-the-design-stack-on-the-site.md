# Show the design stack on the site, not just in the README

- **Commit:** `0c67e8c67211279d6ee7020ba937b22a7697f2ed` (`0c67e8c`)
- **Author:** Claude
- **Date:** 2026-08-16

## Commit message

The README gained a design stack section; the published site still showed
five code tiers and nothing about design, while 5 of 11 skills are now
design skills. The site was the stale half.

Adds DESIGN_STACK to components/lib/skills.ts -- the seven tiers from
CLAUDE.md's design routing table -- and parameterises ContextStack rather
than duplicating it, so both stacks render from one component and one data
source (ADR 0016). Trust gains Evidence / History / Spec, the three labels
the design tiers need and the code tiers don't.

Adds a "Design" nav link; the existing scrollspy picks it up from the
section id with no other change.

Verified in the static export: both stacks render, the nav anchor is
present, and the code stack is unchanged.

## Changes in detail

### `website/app/page.tsx` (modified)

- Second ContextStack instance for the design stack, with its own copy.

### `website/components/ContextStack.tsx` (modified)

- Parameterised rather than duplicated, so both stacks render from one component and one data source (ADR 0016). All props default to the code-stack variant, so existing behaviour is unchanged.

### `website/components/Nav.tsx` (modified)

- "Design" nav link; the existing scrollspy picks it up from the section id with no other change.

### `website/components/lib/skills.ts` (modified)

- DESIGN_STACK added — the seven tiers from CLAUDE.md's design routing table. Trust gains Evidence, History and Spec, the three labels the design tiers need and the code tiers don't.

## Files changed

```
 website/app/page.tsx                | 29 ++++++++++++++++++
 website/components/ContextStack.tsx | 60 +++++++++++++++++++++++++------------
 website/components/Nav.tsx          |  1 +
 website/components/lib/skills.ts    | 56 +++++++++++++++++++++++++++++++++-
 4 files changed, 126 insertions(+), 20 deletions(-)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
