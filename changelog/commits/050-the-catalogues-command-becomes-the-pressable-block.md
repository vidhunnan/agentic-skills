# feat(website): the catalogue's command becomes the pressable block, per spec

- **Commit:** `edf802527d0fcf6e60bd75a76e9b0d1537c62c13` (`edf8025`)
- **Author:** Vidhunnan Murugan
- **Date:** 2026-08-20

## Commit message

From Figma node 2283:2645 (the open row).

The copy control it draws is the hero's primary CopyButton variant to the pixel —
22/14/13 padding, 14px gap, 13.5px --ink on --paper-2, 5px radius, and an action
segment divided by a left border with 14px padding, 11px --mute, uppercase, 0.66px
tracking (which is the 0.06em already in the stylesheet). So this is reuse: the
catalogue row now passes variant="primary" instead of printing a bare <code> beside
a chip button. No new CSS for the control.

The download gains 16px horizontal padding and the row aligns to stretch, so it
matches the copy block's height rather than floating at chip size beside it.

The <code> element still exists inside the button, so the per-surface command
contract in home.spec.ts is unaffected — 26 tests passing.

NOT taken from this node: the surface tags, which it still draws at rounded-[3px]
with 7px padding. That node predates the pill change in 714e0ea, so the pills stand.
Worth knowing the file has both.

## Changes in detail

**13 insertions against 9 deletions** in two source files — the smallest source
footprint of the four Figma-driven commits, and entirely reuse.

### The control is the `CopyButton` that already exists

- The message's method is worth recording as method: it **measured the Figma node
  against the component already shipping** and found them identical — 22/14/13 padding,
  14px gap, 13.5px `--ink` on `--paper-2`, 5px radius, an action segment divided by a
  left border at 14px padding, 11px `--mute`, uppercase. The file's `0.66px` tracking
  *"is the 0.06em already in the stylesheet."*
- The consequence is that the row passes `variant="primary"` — the variant round 4
  added for the hero (`218c91d`, entry 045) — instead of printing a bare `<code>`
  beside a chip button. **No new CSS ships for the control.**

### `website/components/SkillList.tsx` (+8/−2), `Skills.module.css` (+5/−7)

- The open row's command becomes the pressable block. The download gains 16px
  horizontal padding and the row aligns to `stretch`, so it matches the copy block's
  height *"rather than floating at chip size beside it."*
- **The test contract is checked, not assumed**: the `<code>` element still exists
  inside the button, so the per-surface command contract in `home.spec.ts` — the one
  round 5 rewrote (entry 046) — is unaffected.

### The conflict inside the design file, recorded rather than resolved silently

- Node `2283:2645` still draws the surface tags at `rounded-[3px]` with 7px padding.
  **That node predates the pill change** made one commit earlier in `714e0ea` (entry
  049), so the pills stand and the node's tags are not taken.
- The commit says so explicitly — *"Worth knowing the file has both."* This is the
  single most reusable thing in it: a design file is a mutable surface with no version
  history a reader can see, so a code change built from one node has to record which
  parts of that node it declined and why, or the next person reconciles code against
  the file and reintroduces the older shape.

## Files changed

```
 website/components/SkillList.tsx            |  10 ++++++++--
 website/components/Skills.module.css        |  12 +++++-------
 website/public/skills/decisions-logger.zip  | Bin 12702 -> 12702 bytes
 website/public/skills/design-brief.zip      | Bin 5184 -> 5184 bytes
 website/public/skills/design-critique.zip   | Bin 6877 -> 6877 bytes
 website/public/skills/design-decisions.zip  | Bin 9072 -> 9072 bytes
 website/public/skills/design-explore.zip    | Bin 7378 -> 7378 bytes
 website/public/skills/design-language.zip   | Bin 7436 -> 7436 bytes
 website/public/skills/design-setup.zip      | Bin 9933 -> 9933 bytes
 website/public/skills/exploration-log.zip   | Bin 5266 -> 5266 bytes
 website/public/skills/handoff-generator.zip | Bin 8690 -> 8690 bytes
 website/public/skills/repo-setup.zip        | Bin 9809 -> 9809 bytes
 website/public/skills/skill-scaffold.zip    | Bin 8428 -> 8428 bytes
 13 files changed, 13 insertions(+), 9 deletions(-)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
