# feat(website): surface tags become pills, and there is only one Tags now

- **Commit:** `714e0ea3f54b0b4e4a204be498e397033dcc9aff` (`714e0ea`)
- **Author:** Vidhunnan Murugan
- **Date:** 2026-08-20

## Commit message

From the updated Figma (node 2265:1241): fully rounded, horizontal padding 7px →
12px. Border, colour and 11px size unchanged.

The file carries 35px on the Code tag and 9999px on the Chat tag. At a 21px tag
those render identically, so the code uses one value and says why rather than
copying an inconsistency into CSS.

Also collapses a duplicate I introduced in round 5: SkillList had its own inline
Tags function with its own copy of the styles in Skills.module.css, while
InstallSteps used the shared components/Tags.tsx. Two implementations of the same
element is how the catalogue and the installer drift apart — the pill change would
have had to be made twice, and one of them would eventually get missed. SkillList
now imports the shared component and the duplicate CSS is gone.

26 tests passing.

## Changes in detail

**5 insertions against 27 deletions** across three source files — a net deletion, and
the deletion is the point.

### The pill — `Tags.module.css` (+4/−2)

- Fully rounded; horizontal padding **7px → 12px**. Border, colour and the 11px size
  are unchanged.
- **The commit refuses to copy an inconsistency out of the design file.** The Figma
  node carries `35px` on the Code tag and `9999px` on the Chat tag; at a 21px-tall tag
  those render identically. The code uses one value *"and says why rather than copying
  an inconsistency into CSS"* — the resolution is recorded in the stylesheet, so the
  next person to diff code against the file finds the answer instead of the question.

### The duplicate — `SkillList.tsx` (−14), `Skills.module.css` (−11)

- Round 5 (`10b64af`, entry 046) shipped **two implementations of the same element**:
  `SkillList` carried its own inline `Tags` function with its own `.tags`/`.tag` rules
  in `Skills.module.css`, while `InstallSteps` used the shared `components/Tags.tsx`.
  Confirmed against the tree at `10b64af` — the inline function is there.
- `SkillList` now imports the shared component; the duplicated CSS is deleted.
- **The commit states the failure mode it is pre-empting**, and it is the one this
  round would have hit immediately: *"Two implementations of the same element is how
  the catalogue and the installer drift apart — the pill change would have had to be
  made twice, and one of them would eventually get missed."*

## Files changed

```
 website/components/SkillList.tsx            |  15 +--------------
 website/components/Skills.module.css        |  11 -----------
 website/components/Tags.module.css          |   6 ++++--
 website/public/skills/decisions-logger.zip  | Bin 12702 -> 12702 bytes
 website/public/skills/design-brief.zip      | Bin 5184 -> 5184 bytes
 website/public/skills/design-critique.zip   | Bin 6877 -> 6877 bytes
 website/public/skills/design-decisions.zip  | Bin 9072 -> 9072 bytes
 website/public/skills/design-explore.zip    | Bin 7378 -> 7378 bytes
 website/public/skills/design-language.zip   | Bin 7436 -> 7436 bytes
 website/public/skills/design-setup.zip      | Bin 9933 -> 9933 bytes
 website/public/skills/exploration-log.zip   | Bin 5266 -> 5266 bytes
 website/public/skills/handoff-generator.zip | Bin 8690 -> 8690 bytes
 website/public/skills/repo-setup.zip        | Bin 9809 -> 9809 bytes
 website/public/skills/skill-scaffold.zip    | Bin 8428 -> 8428 bytes
 14 files changed, 5 insertions(+), 27 deletions(-)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
