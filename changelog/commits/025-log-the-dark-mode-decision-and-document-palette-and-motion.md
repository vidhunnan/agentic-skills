# Log the dark-mode decision and document the palette and motion

- **Commit:** `6f6d6c926f63aaf61ef75442df73af9447e1aa54` (`6f6d6c9`)
- **Author:** Claude
- **Date:** 2026-08-16

## Commit message

The Phase C code landed with Phase B; this is the record it owes under this
repo's own protocols -- a design system change is a decision, not a media
query.

- design/decisions/0003 -- follow prefers-color-scheme with a palette that is
  RE-LIT, not inverted. A mechanical swap puts cobalt at 1.90:1 and the
  redline at 2.90:1 on the dark ground: an inaccessible page. Both accents
  are lifted instead. Records what it costs: the paper metaphor ADR 0017
  chose is diluted, there are now two palettes to keep honest, and the dark
  accents no longer match the talk deck's cobalt.
- design/system/palette.md -- all ten tokens in both palettes with computed
  contrast ratios, plus the when-NOT-to-use section: never --mute for
  anything that matters, never --red decoratively (it means hypothesis and
  failure), never a new colour without computing both ratios. The last one is
  why --mute shipped at 3.08:1 and --red at 4.43:1 -- both were picked by eye.
- design/specs/motion.md -- the site's entire motion vocabulary: four
  behaviours, one easing curve, the stagger rule, and the reduced-motion
  fallback. Includes why the stagger is per-list and not per-page (a
  page-wide counter would give the last row a 900ms delay).

Verification, all eight items:
  parity 11/11 across marketplace, disk, README rows, install lines, skills.ts
  all 12 rendered paths resolve AND appear in a CLAUDE.md routing table
  no bare .reveal{opacity:0}; hidden state gated behind .js-reveal
  all 11 skills present in the static HTML with scripts stripped
  both palettes clear AA on every token, computed not estimated
  zero hardcoded --paper rgba literals left; --scrim in use
  nav links are a scroll rail under 720px, not display:none

The design stack now has real content in five of its six tiers -- written by
the skills it documents, which was the point.

## Changes in detail

### `design/decisions/0003-the-site-follows-the-system-colour-scheme.md` (new)

- Follow prefers-color-scheme with a re-lit rather than inverted palette — a mechanical swap puts cobalt at 1.90:1 and the redline at 2.90:1 on the dark ground. Records the cost: the paper metaphor is diluted, two palettes must be kept honest, and the dark accents no longer match the talk deck.

### `design/decisions/README.md` (modified)

- Index row for 0003.

### `design/specs/motion.md` (new)

- The site's entire motion vocabulary: four behaviours, one easing curve, the per-list stagger rule and why it is not per-page, and the reduced-motion fallback.

### `design/system/palette.md` (new)

- All ten tokens in both palettes with computed contrast ratios, and a when-not-to-use section — never --mute for anything that matters, never --red decoratively, never a new colour without computing both ratios.

## Files changed

```
 ...03-the-site-follows-the-system-colour-scheme.md | 85 +++++++++++++++++++
 design/decisions/README.md                         |  3 +-
 design/specs/motion.md                             | 75 +++++++++++++++++
 design/system/palette.md                           | 95 ++++++++++++++++++++++
 4 files changed, 257 insertions(+), 1 deletion(-)
```

---

_Generated by `/changelog-tracker` from `git log -1` and `git show --stat`. Every
fact here comes from git: **faithful, not generative.**_
